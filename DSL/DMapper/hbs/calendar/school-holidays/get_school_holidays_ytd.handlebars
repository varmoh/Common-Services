{
  "result": "Kõik koolivaheajad aasta algusest kuni tänaseni on\\n\\n{{#each data}}{{#unless @last}}{{formatDate this.startDate}} - {{formatDate this.endDate}} {{this.name.[0].text}}\\n{{else}}{{formatDate this.startDate}} - {{formatDate this.endDate}} {{this.name.[0].text}}{{/unless}}{{/each}}"
}
