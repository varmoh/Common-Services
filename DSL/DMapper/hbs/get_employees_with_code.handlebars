{
    "employees": [
    {{#each hits}}
            {
              "text": "{{ _source.workers }} inimest töötab firmas {{ _source.name }}",
              "result": "{{ _source.name }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
