{
    "employees": [
    {{#each hits}}
            {
              "employees": {{ _source.workers }},
              "name": "{{ _source.name }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
