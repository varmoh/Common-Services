{
    "revenue": [
    {{#each hits}}
            {
              "result": "{{ _source.turnover }}",
              "text": "{{ _source.name }} käive oli {{ formatToReadableNumber _source.turnover }} eurot "
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
