{
    "national_taxes": [
    {{#each hits}}
            {
              "result": "{{ _source.national_taxes }}",
              "text": "{{ _source.name }} riiklikud maksud olid {{ formatToReadableNumber _source.national_taxes }} eurot"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
