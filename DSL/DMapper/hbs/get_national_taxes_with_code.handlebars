{
    "national_taxes": [
    {{#each hits}}
            {
              "result": "{{ _source.national_taxes }}",
              "text": "{{ _source.name }} on {{../year}}. aasta eelmisel kvartalil tasunud riiklikke makse kokku {{ formatToReadableNumber _source.national_taxes }} eurot."
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
