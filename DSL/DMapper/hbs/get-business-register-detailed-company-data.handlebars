{
    "ariregistri_kood": "{{ keha.ariregistri_kood }}",
    "nimi": "{{ keha.nimi }}",
    "sidevahendid": [
    {{#each keha.yldandmed.sidevahendid.item }}
        {
            "liik": "{{ liik }}",
            "sisu": "{{ sisu }}"
        }{{#unless @last}},{{/unless}}
    {{/each}}
    ],
    "text": "{{keha.nimi}} firma telefon on {{#each keha.yldandmed.sidevahendid.item}}{{#if (eq liik 'TEL')}}{{sisu}}{{/if}}{{/each}} ja email on {{#each keha.yldandmed.sidevahendid.item}}{{#if (eq liik 'EMAIL')}}{{sisu}}{{/if}}{{/each}}"
}
